# iMenu 2026 — ملفات محلية لا تُرفع

# مجلد مؤقت لملفات القفل المتبقية من جلسات git — يُحذف يدويًا
/_to_delete/

# إعدادات محلية لأدوات المطوّر
/.claude/

# أسرار البيئة — لا تُرفع أبدًا
.env
.env.*
!.env.example

# سجلات وأدوات
npm-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
